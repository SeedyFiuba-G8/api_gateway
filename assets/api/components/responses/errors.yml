# Common errors

components:
  responses:
    error:
      400:
        description: >
          Bad Request
        content:
          $ref: '#/definitions/error'
      404:
        description: >
          Not Found
        content:
          $ref: '#/definitions/error'
      409:
        description: >
          Internal Server Error
        content:
          $ref: '#/definitions/error'
