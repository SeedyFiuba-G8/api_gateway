responses:
  health:
    200:
      description: 'Microservice general health state reported.'
      content:
        application/json:
          schema:
            type: object
            properties:
              users:
                type: string
                example: 'UP'
              core:
                type: string
                example: 'UP'
              apikeys:
                type: string
                example: 'UP'
