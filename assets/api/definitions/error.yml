definitions:
  error:
    application/json:
      schema:
        type: object
        properties:
          name:
            type: string
            example: 'Some error name.'
          message:
            type: string
            example: 'Some error message.'
          data:
            type: object
